node_modules/
dist/
build/
out/

# Generated by `npm run icons` (tools/make-icons.js).
src/assets/*.png

# The old tkinter app's data, left in place so the importer can still find it.
*.db
settings.json

*.log
Thumbs.db

# Left over from the tkinter version, which is still installed on this machine
# and still has to run until this one has replaced it.
__pycache__/
*.pyc
*.spec
dist - Copy/
